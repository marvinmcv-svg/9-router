# ─── Required ────────────────────────────────────────────────────────────────
# The kernel runs on claude-opus-5. Get a key at console.anthropic.com.
ANTHROPIC_API_KEY=

# ─── Google Workspace (Gmail, Calendar, Drive) ───────────────────────────────
# Create an OAuth 2.0 Web application client at console.cloud.google.com and
# enable the Gmail, Calendar and Drive APIs. Add this authorized redirect URI:
#   http://localhost:3000/api/auth/google       (development)
#   https://<your-domain>/api/auth/google       (production)
# Then click Connect in the Connectors panel to grant access.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ─── Development infrastructure ──────────────────────────────────────────────
# Fine-grained PAT: github.com/settings/tokens
GITHUB_TOKEN=
# vercel.com/account/tokens
VERCEL_TOKEN=
# railway.app/account/tokens
RAILWAY_TOKEN=

# ─── State ───────────────────────────────────────────────────────────────────
# Optional locally (state falls back to ./.jarvis), REQUIRED on serverless
# hosts, where the filesystem is ephemeral and not shared between invocations.
# Run supabase/schema.sql against the project first.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# ─── Deployment ──────────────────────────────────────────────────────────────
# Public origin, used to build the Google OAuth redirect URI.
JARVIS_BASE_URL=http://localhost:3000
# IANA timezone used by scheduled routines.
JARVIS_TIMEZONE=UTC
# Shared secret the scheduler sends as `Authorization: Bearer <secret>`.
# Routines refuse to run in production without this.
JARVIS_CRON_SECRET=

# ─── Model (the brain) ───────────────────────────────────────────────────────
# JARVIS is provider-independent. Pick one:
#
#   anthropic             api.anthropic.com — set JARVIS_API_KEY to sk-ant-...
#   anthropic-compatible  GLM / Z.ai, LiteLLM, any Anthropic-shaped gateway
#   openai-compatible     Ollama, vLLM, OpenRouter, llama.cpp, LM Studio
#
# All of this is also editable at runtime in the Model panel, which stores it
# in kernel state and overrides whatever is set here.
JARVIS_PROVIDER=anthropic
JARVIS_BASE_URL_MODEL=
JARVIS_API_KEY=
JARVIS_MODEL=

# Extended thinking. Defaults on for Anthropic, off elsewhere — most
# compatible gateways reject the parameter.
JARVIS_THINKING=

# ─── Workspace ───────────────────────────────────────────────────────────────
# Root for fs.* and shell.exec. Every model-supplied path is resolved and
# checked against this; symlinks that point outside are refused. Defaults to
# the process working directory — set it deliberately.
JARVIS_WORKSPACE=
